org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.builtInKotlin=true
# Legacy AGP DSL. Must stay false: dev.flutter.flutter-gradle-plugin casts to
# AbstractAppExtension, which the new DSL doesn't provide. AGP removes the
# opt-out in 10.0, so this needs a Flutter-side fix first.
android.newDsl=false
